#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Quality gate (Google/OpenAI standard): fail fast on lint, unit tests, and security audit.
npm run lint || exit 1
npm test || exit 1
npm run build || exit 1
npm run audit:security || exit 1
